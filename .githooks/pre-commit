#!/bin/sh
set -eu
root=$(git rev-parse --show-toplevel)
policy_python=$(git config --get bethkit.policyPython) || {
    echo 'Install repository hooks with python scripts/install_git_hooks.py.' >&2
    exit 1
}
exec "$policy_python" "$root/scripts/git_policy_hooks.py" pre-commit
